export type ActionButtonType = {
    image: string;
    action: any;
};

export type ButtonType = {
    placeholder: string;
    action: any;
};

export type CryptoButtonType = {
    userCount: number;
    coinPrice: number;
    iconUri: string;
    uuid: string;
    name: string;
    symbol: string;
};
